create table results (
	runnerid int primary key,
	firstname varchar,
	lastname varchar,
	age int,
	bib int,
	birthdate varchar,
	gender varchar,
	overallplace int,
	overalltime varchar
);

select * from results;
